apiVersion: v1
kind: Service
metadata:
  name: rick-roll
  labels:
    app: rick-roll
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: rick-roll
  type: ClusterIP
